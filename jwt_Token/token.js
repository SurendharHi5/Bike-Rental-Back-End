// const jwt = require("jsonwebtoken");
// const dotenv = require("dotenv")
// dotenv.config();

// const generateToken = (user) => jwt.sign({id: user.id}, process.env.JWT_SECRET, { expiresIn: "2d"})


// module.exports = generateToken